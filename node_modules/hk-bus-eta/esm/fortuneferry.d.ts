import type { Eta, EtaDb, RouteListEntry, StopList } from "./type";
interface fetchEtasProps {
    stops: {
        fortuneferry: string[];
    };
    seq: number;
    stopList: StopList;
    freq: RouteListEntry["freq"];
    holidays: EtaDb["holidays"];
    serviceDayMap: EtaDb["serviceDayMap"];
}
export default function fetchEtas({ stops: { fortuneferry }, seq, holidays, freq, serviceDayMap, }: fetchEtasProps): Promise<Eta[]>;
export {};
