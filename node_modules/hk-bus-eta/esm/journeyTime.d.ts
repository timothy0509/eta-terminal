import { RouteListEntry, StopList } from "./type";
/**
 * Fetch Journey time in minute for a route
 * @param {Object}
 * @returns {number} journey time in minute
 */
export declare function fetchEstJourneyTime({ route, stopList, startSeq, endSeq, batchSize, signal, }: {
    route: RouteListEntry;
    stopList: StopList;
    startSeq: number;
    endSeq: number;
    batchSize?: number;
    signal?: AbortSignal | null;
}): Promise<number>;
