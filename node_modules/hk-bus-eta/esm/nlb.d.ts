import type { Eta, RouteListEntry } from "./type";
interface fetchEtasProps {
    stopId: string;
    nlbId: RouteListEntry["nlbId"];
}
export default function fetchEtas({ stopId, nlbId, }: fetchEtasProps): Promise<Eta[]>;
export {};
